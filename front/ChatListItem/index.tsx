import React from "react";
import { View } from "react-native";
import { ChartRoom } from "../types";

const ChatListItem = (chartRooms: ChartRoom) => {
    return <View></View>;
};

export default ChatListItem;
